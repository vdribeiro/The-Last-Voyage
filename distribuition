# Bump Version
    build.gradle.kts
    info.plist
    project.pbxproj
    configs.json
# Upload Gists
    gh gist edit hash *.json
# Set Flags
    set TLV object flags
# Pod install
    rm -rf Pods
    rm Podfile.lock
    pod install
# Compile
    ./gradlew clean bundleRelease
    ./gradlew clean packageDmg
